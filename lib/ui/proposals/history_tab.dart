import 'package:flutter/material.dart';

class history_tab extends StatelessWidget {
  const history_tab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Zeeshan'));
  }
}
